import { PhoneCommaFormatDirective } from './phone-comma-format.directive';

describe('PhoneCommaFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new PhoneCommaFormatDirective();
    expect(directive).toBeTruthy();
  });
});
