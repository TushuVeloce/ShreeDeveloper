import { AmountCommaFormatDirective } from './amount-comma-format.directive';

describe('AmountCommaFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountCommaFormatDirective();
    expect(directive).toBeTruthy();
  });
});
