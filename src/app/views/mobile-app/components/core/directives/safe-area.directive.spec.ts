import { SafeAreaDirective } from './safe-area.directive';

describe('SafeAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeAreaDirective();
    expect(directive).toBeTruthy();
  });
});
